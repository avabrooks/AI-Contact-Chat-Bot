# Anthropic API key used for plain-text -> SQL -> plain-English answers
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# Basic-auth gate so only IT can reach the bot (temporary measure --
# for real use, put this behind your VPN/intranet and/or SSO instead)
IT_BOT_USER=it
IT_BOT_PASS=changeme
